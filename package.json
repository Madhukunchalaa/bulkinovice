{
  "name": "bulk-invoice-generator-root",
  "version": "1.0.0",
  "private": true,
  "main": "backend/dist/index.js",
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate && npm run build",
    "start": "cd backend && npm run start"
  }
}
